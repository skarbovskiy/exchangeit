define([
    'main.module'
], function (main) {
    'use strict';

    main.controller('Catalog', ['$scope', function ($scope) {
        var Catalog = {};

        $scope.Catalog = Catalog;
    }]);
});